/* CCAR-F Mock Exam 2 — 60 fresh items, 4 scenarios, official weightings.
   Distribution: D1×16, D2×11, D3×12, D4×12, D5×9.
   ans is an array of correct option indices (length>1 = multiple-response). */
window.MOCK2 = {
  id: 'MOCK2',
  title: 'Mock Exam 2 — CCAR-F',
  minutes: 120,
  weights: { 1: .27, 2: .18, 3: .20, 4: .20, 5: .15 },
  scenarios: [
    { name: 'Customer Support Resolution Agent',
      blurb: 'You are building a support agent with the Claude Agent SDK. It handles returns, billing disputes and account issues via MCP tools (get_customer, lookup_order, process_refund, escalate_to_human). Target: 80%+ first-contact resolution while knowing when to escalate.' },
    { name: 'Multi-Agent Research System',
      blurb: 'A coordinator delegates to specialised subagents: one searches the web, one analyses documents, one synthesises findings, one generates reports. The system produces comprehensive, cited reports on requested topics.' },
    { name: 'Code Generation with Claude Code',
      blurb: 'Your team uses Claude Code for generation, refactoring, debugging and documentation. You are integrating it into the workflow with custom slash commands, CLAUDE.md configuration, and plan mode, and into CI for automated review.' },
    { name: 'Structured Data Extraction Pipeline',
      blurb: 'You extract structured data from unstructured documents, validate output against JSON schemas, and feed downstream systems. The pipeline must handle edge cases gracefully and maintain high accuracy at volume.' }
  ],
  questions: [
    /* ============ SCENARIO 1 — Customer Support (D1/D2/D5) ============ */
    { scen:0, d:1, ts:'1.1', ans:[2],
      stem:'Your agent loop terminates as soon as the model’s response contains any text block. Users report it stops mid-task, before calling the tools needed to finish. What is the correct termination condition?',
      opts:[
        'Continue only while the response contains no text block, since text signals the agent is talking rather than working.',
        'Continue until a fixed cap of eight iterations is reached, which bounds cost while allowing multi-tool tasks.',
        'Continue while stop_reason is "tool_use" and terminate when it is "end_turn".',
        'Continue until the response text contains an explicit completion phrase such as "task complete".'],
      why:'stop_reason is the only reliable signal: "tool_use" means execute and loop, "end_turn" means stop. A turn can contain both text and a tool_use block, so checking for text terminates early — exactly the bug described.' },
    { scen:0, d:1, ts:'1.4', ans:[0],
      stem:'Compliance requires that no refund above $500 is ever issued without human approval. Which mechanism guarantees this?',
      opts:[
        'A PreToolUse hook that blocks process_refund calls above $500 and redirects to escalation.',
        'A system prompt rule stating that refunds above $500 must always be escalated to a human first.',
        'Few-shot examples showing the agent escalating large refunds instead of processing them.',
        'A post-hoc audit log that flags any refund above $500 for next-day review by a manager.'],
      why:'A hard monetary ceiling is an invariant, so it needs deterministic enforcement. A PreToolUse hook blocks the action before it happens. Prompt and few-shot approaches are probabilistic; an audit log detects violations after the money has moved.' },
    { scen:0, d:1, ts:'1.5', ans:[1],
      stem:'get_customer returns account age as a Unix timestamp, while lookup_order returns dates as ISO 8601 strings. The agent occasionally computes wrong date differences across the two. What is the appropriate fix?',
      opts:[
        'Add a system prompt note explaining both formats and asking the model to convert carefully.',
        'Add a PostToolUse hook that normalises both tools’ dates to a single format before the model reads them.',
        'Replace both MCP tools with new ones that agree to emit the same date format at the source.',
        'Instruct the agent to call a separate convert_date tool after every get_customer and lookup_order.'],
      why:'Deterministic conversion belongs in code that runs every time. PostToolUse fires after the tool returns and rewrites the output, so the model only ever sees one format. Prompt guidance runs at an error rate forever; rebuilding the backend tools is out of your control and heavier than needed.' },
    { scen:0, d:5, ts:'5.2', ans:[0],
      stem:'A customer writes: "This is the third time I’ve contacted you and I am furious." The underlying issue is a standard damaged-item replacement with photo evidence attached. What should the agent do?',
      opts:[
        'Acknowledge the frustration and resolve the replacement, escalating only if the customer then insists on a human.',
        'Escalate immediately, since a customer who is furious after three contacts clearly needs a human agent.',
        'Run sentiment analysis and escalate automatically because the negative sentiment exceeds a safe threshold.',
        'Ask the customer to confirm they are not requesting a human before proceeding with the replacement.'],
      why:'Frustration is not an escalation trigger — the case is within policy and the agent’s capability. Acknowledge the feeling and resolve; escalate only if the customer explicitly reiterates a request for a human. Sentiment does not correlate with complexity.' },
    { scen:0, d:5, ts:'5.2', ans:[2],
      stem:'get_customer returns two accounts matching the caller’s stated name. What is the correct behaviour?',
      opts:[
        'Escalate to a human, since acting on either account risks modifying the wrong customer’s data.',
        'Select the account with more recent activity, as it is the more likely match statistically.',
        'Ask the customer for an additional identifier such as an order number or email address.',
        'Proceed with read-only operations on both accounts until the ambiguity resolves itself.'],
      why:'Multiple matches call for clarification — request another identifier. It resolves the ambiguity in one turn and preserves first-contact resolution. Heuristic selection risks the wrong account; escalation dumps a solvable problem on a human.' },
    { scen:0, d:5, ts:'5.1', ans:[0],
      stem:'A long billing conversation is progressively summarised to stay in budget, and the agent starts quoting a vague "recent charge" instead of the exact $148.50 dispute amount and date. What is the fix?',
      opts:[
        'Extract transactional facts (amount, date, order id, status) into a persistent case-facts block included with every prompt.',
        'Disable summarisation and instead truncate the oldest turns once the conversation grows too long.',
        'Increase the summary length budget so more of the original exact wording survives compression.',
        'Switch to a larger-context model so the full conversation never needs to be summarised at all.'],
      why:'Compression flattens specifics. The fix is to give the facts that must survive a home outside the summarised history — a structured case-facts block on every prompt. Longer summaries and bigger windows only delay the same loss.' },
    { scen:0, d:2, ts:'2.2', ans:[0],
      stem:'process_refund fails because the order is outside the 30-day return window. What should the tool return so the agent responds well?',
      opts:[
        'isError true, errorCategory "business", isRetryable false, the specific policy and a customer-friendly explanation.',
        'isError true, errorCategory "transient", isRetryable true, so the agent retries in case the window updates.',
        'A generic "refund failed" string, leaving the agent to infer the cause from the conversation context.',
        'isError true with a machine code REFUND_WINDOW_EXCEEDED and no human-readable message attached.'],
      why:'A policy violation is a business error: no retry helps. The blueprint calls for isRetryable false plus a customer-friendly explanation, so the agent can relay the refusal appropriately and offer alternatives. A bare code withholds the specifics the model needs to explain it.' },
    { scen:0, d:2, ts:'2.1', ans:[1],
      stem:'Logs show the agent calls get_customer when users ask about specific orders. Both get_customer and lookup_order have one-line descriptions and accept similar identifiers. What is the best first step?',
      opts:[
        'Add eight few-shot examples routing order queries to lookup_order in the system prompt.',
        'Expand each description with input formats, example queries, edge cases, and when to use it versus the other.',
        'Merge the two tools into one lookup_entity that resolves the backend internally from the identifier.',
        'Add a keyword router that inspects each message and pre-selects the tool before the model runs.'],
      why:'Descriptions are the primary selection mechanism and these are minimal. Expanding them acts directly on the root cause at low cost — the right first step. Consolidation is heavier than "first step" warrants; few-shot and routers add overhead without fixing the description gap.' },
    { scen:0, d:1, ts:'1.4', ans:[0,2],
      stem:'A customer message contains three separate issues: a wrong-size return, a duplicate charge, and an address update. Which practices does the blueprint prescribe? (Select 2.)',
      opts:[
        'Decompose the message into distinct items and investigate each before synthesising one unified resolution.',
        'Handle only the first issue this turn and ask the customer to resubmit the others separately.',
        'Compile a structured handoff (customer id, each issue, resolution) if any single item must escalate.',
        'Escalate the entire message to a human because multi-issue requests exceed autonomous scope.'],
      why:'Multi-concern requests are decomposed, investigated in parallel using shared context, then synthesised into one response. If one item must escalate, a structured handoff carries the findings. Multi-issue is not itself an escalation trigger, and forcing resubmission shifts burden to the customer.' },
    { scen:0, d:2, ts:'2.3', ans:[1],
      stem:'The agent has 16 tools, many unrelated to support (analytics exporters, admin utilities), and tool selection is unreliable. What does the blueprint recommend?',
      opts:[
        'Keep all 16 but reorder them so the four support tools appear first for selection preference.',
        'Scope the agent to the four support tools its role needs, removing the unrelated ones.',
        'Add a dispatcher tool that takes an action name, hiding the 16 behind a single entry point.',
        'Accept the 16 tools since unused ones are inert and do not affect the agent’s behaviour.'],
      why:'Tool count itself degrades selection — every tool is a candidate answer to each choice. Scope the agent to the tools its role needs. Reordering relies on a preference that is not a contract; a dispatcher hides the same wide choice in a parameter.' },
    { scen:0, d:5, ts:'5.3', ans:[0],
      stem:'A subagent that verifies account status times out, and its own retries are exhausted. What should propagate to the coordinator?',
      opts:[
        'Structured error context: failure type, the query attempted, any partial results, and possible alternatives.',
        'A generic "verification unavailable" status so the coordinator can decide quickly.',
        'An empty verification result marked successful, so the workflow is not interrupted.',
        'An exception that terminates the current customer interaction to avoid acting on incomplete data.'],
      why:'Recover local, then propagate structure. The coordinator needs failure type, what was attempted, partials, and alternatives to recover intelligently. A generic status hides context; a suppressed error plants misinformation; termination over-reacts to a recoverable failure.' },
    { scen:0, d:1, ts:'1.1', ans:[0],
      stem:'After Claude returns a tool_use block and your code runs the tool, how must the result be sent back for the loop to continue correctly?',
      opts:[
        'Append the assistant turn unchanged, then a new user message with a tool_result block whose tool_use_id matches.',
        'Append a new assistant message containing the tool output, so the transcript shows the tool ran.',
        'Send the tool output as a plain user text message, letting the model read it as ordinary input.',
        'Replace the assistant’s tool_use block with the result to keep the conversation history compact.'],
      why:'The assistant turn (with its tool_use block) goes back verbatim, then the result returns in a user message as a tool_result whose tool_use_id pairs to the original id. Altering or omitting the assistant turn breaks the pairing.' },

    /* ============ SCENARIO 2 — Multi-Agent Research (D1/D2/D5) ============ */
    { scen:1, d:1, ts:'1.3', ans:[0],
      stem:'Your coordinator defines three subagents but never delegates — it answers research questions itself. What is the most likely configuration cause?',
      opts:[
        'allowedTools does not include "Task", so subagent invocations are never approved.',
        'The subagents lack a model field, so they cannot be instantiated by the coordinator.',
        'The subagents specify a tools array, which blocks the coordinator from invoking them.',
        'The coordinator prompt states goals rather than procedures, suppressing delegation.'],
      why:'Subagents spawn through the Task tool; if "Task" is absent from allowedTools the invocation is not approved and Claude does the work itself. model is optional; a tools array restricts the subagent, not whether it can be called.' },
    { scen:1, d:1, ts:'1.3', ans:[0],
      stem:'The synthesis subagent produces reports that reference "the sources found" but contain no actual findings, even though the search subagent returned good results to the coordinator. Why?',
      opts:[
        'The coordinator did not include the search findings in the synthesis subagent’s prompt; subagents inherit no parent context.',
        'The synthesis subagent’s context filled and evicted the findings it received when it was spawned.',
        'The search and synthesis subagents hold conflicting system prompts, so the data was discarded.',
        'The findings were too long for the coordinator to relay within a single Task tool call.'],
      why:'A subagent starts fresh; the only channel from parent to child is the Task prompt string. If the coordinator does not paste the findings in, synthesis has never seen them. Context is not silently evicted after spawn.' },
    { scen:1, d:1, ts:'1.2', ans:[1],
      stem:'A report on "renewable energy adoption" covers only solar, omitting wind, hydro and geothermal. Coordinator logs show three subtasks: "solar panel costs", "residential solar", "solar incentives". Every subagent completed successfully. Where is the fault?',
      opts:[
        'The synthesis subagent failed to detect and flag the missing coverage areas in its output.',
        'The coordinator’s task decomposition was too narrow, so the subtasks never covered the topic.',
        'The web search subagent used queries too restrictive to reach non-solar energy sources.',
        'The document analysis subagent filtered out non-solar material with over-strict relevance rules.'],
      why:'The logs show the topic was decomposed into three solar subtasks. Every subagent then executed correctly within its assigned scope. This is the narrow-decomposition failure — confident, coherent, incomplete output caused by what was asked, not by execution.' },
    { scen:1, d:1, ts:'1.2', ans:[0],
      stem:'To reduce duplicated effort across two research subagents covering overlapping ground, what should the coordinator do?',
      opts:[
        'Partition the scope, assigning each subagent distinct subtopics or source types.',
        'Let both subagents search freely and deduplicate their combined findings afterwards.',
        'Run the two subagents sequentially so the second can avoid whatever the first covered.',
        'Merge the two subagents into one so there is no possibility of overlapping work.'],
      why:'Scope partitioning — distinct subtopics or source types per agent — minimises duplication by design. Post-hoc dedup wastes the duplicated work; sequential execution loses parallelism; merging discards the specialisation that motivated multiple agents.' },
    { scen:1, d:1, ts:'1.2', ans:[0],
      stem:'The coordinator produces a synthesis, then notices whole subtopics are thinly covered. What is the prescribed pattern?',
      opts:[
        'Iteratively re-delegate to search and analysis with targeted queries, then re-invoke synthesis until coverage is sufficient.',
        'Accept the synthesis as final, since re-running agents risks introducing contradictions between passes.',
        'Expand the synthesis prompt with instructions to write more thoroughly about the thin areas.',
        'Lower the quality bar for this topic because the available sources evidently cannot support it.'],
      why:'Iterative refinement: the coordinator evaluates synthesis for gaps, re-delegates with targeted queries, and re-invokes synthesis until coverage is adequate. Asking synthesis to "write more" cannot manufacture evidence it never received.' },
    { scen:1, d:1, ts:'1.3', ans:[0],
      stem:'To run three independent research subagents concurrently rather than one after another, the coordinator must:',
      opts:[
        'Emit all three Task tool calls within a single response.',
        'Emit one Task call per response across three consecutive turns for the runtime to batch.',
        'Set a parallel:true flag on each of the three AgentDefinition entries.',
        'Spawn one subagent and instruct it to spawn the other two from within its own context.'],
      why:'Concurrency comes from multiple Task tool_use blocks in one assistant turn — the parallel tool-call mechanic. One call per turn is accidentally sequential: same output, sum-of-latencies instead of slowest-of.' },
    { scen:1, d:2, ts:'2.3', ans:[0],
      stem:'The synthesis subagent frequently needs simple fact-checks (dates, names) and currently round-trips through the coordinator to the search agent, adding 40% latency. 85% of these are trivial lookups. Best fix?',
      opts:[
        'Give synthesis a scoped verify_fact tool for simple lookups; complex verifications still route through the coordinator.',
        'Give synthesis access to the full web search toolset so it can verify anything without round trips.',
        'Have synthesis batch all verification needs and send them to the coordinator at the end of its pass.',
        'Have the search agent pre-cache extra context around every source in case synthesis needs it.'],
      why:'Least privilege sized to the common case: a narrow verify_fact covers the 85%, the coordinator handles the hard 15%. Full toolset over-provisions and degrades selection; batching creates blocking dependencies; speculative caching cannot predict needs.' },
    { scen:1, d:5, ts:'5.6', ans:[0],
      stem:'The final report states "adoption rose 14%" but nobody can tell which source supports it, and a conflicting 9% figure was silently dropped in synthesis. What structural change prevents this?',
      opts:[
        'Require each finding to carry a claim–source mapping (excerpt, URL, date) that synthesis must preserve, annotating conflicts rather than dropping them.',
        'Instruct the synthesis agent to double-check every statistic against its sources before writing the report.',
        'Add a fact-checking agent that re-searches the web for each figure in the finished report.',
        'Have the coordinator retain every subagent’s verbatim output so figures can be traced after delivery.'],
      why:'Attribution loss is structural, so the fix is structural: claim–source mappings preserved and merged through synthesis, with conflicting values annotated (both, with attribution and dates). A prompt to "double-check" cannot restore sourcing already discarded.' },
    { scen:1, d:5, ts:'5.6', ans:[0],
      stem:'Two credible sources report different market-size statistics. How should the synthesis agent handle the conflict?',
      opts:[
        'Present both values with their source attributions and collection dates, letting the reader see the discrepancy.',
        'Select the value from the source with the higher domain authority and omit the other.',
        'Average the two figures to produce a single representative number for the report.',
        'Omit the statistic entirely, since conflicting sources make any single figure unreliable.'],
      why:'Conflicting statistics from credible sources are annotated with both attributions rather than arbitrarily resolved — and dates prevent temporal differences being read as contradictions. Picking, averaging, or omitting all destroy information the reader needs.' },
    { scen:1, d:5, ts:'5.3', ans:[0],
      stem:'A search subagent queries a database for "2025 grants" and receives a well-formed empty result. How should this be reported to the coordinator?',
      opts:[
        'As a successful query with no matches — a valid empty result, distinct from an access failure.',
        'As a retryable transient error, since an empty result likely indicates a degraded index.',
        'As a coverage gap, flagging that the grants topic could not be supported by any source.',
        'As a failure with partial results so the coordinator can judge whether the empty set is trustworthy.'],
      why:'A successful query returning nothing is an answer, not a failure. Reporting it as any kind of error sends the coordinator chasing a non-existent problem. Coverage gaps are for topics that could not be checked because sources were unavailable — not for genuine empties.' },
    { scen:1, d:2, ts:'2.4', ans:[0],
      stem:'The research system should expose a catalog of available document collections so agents stop making exploratory list calls to discover what exists. What MCP feature fits?',
      opts:[
        'MCP resources exposing the content catalog, giving agents visibility without tool calls.',
        'An MCP tool named list_collections that returns the whole catalog on each invocation.',
        'A section of the coordinator’s system prompt listing every collection by name.',
        'A PostToolUse hook that appends the catalog to the result of every search call.'],
      why:'MCP resources exist to expose content catalogs (collections, schemas, summaries) so agents see what is available without exploratory tool calls. A list tool spends a round trip and selection budget on what resources give for free.' },
    { scen:1, d:5, ts:'5.4', ans:[0],
      stem:'An overnight multi-agent research run must survive a crash without redoing hours of work. Which design does the blueprint name?',
      opts:[
        'Each agent exports structured state to a known location; on resume the coordinator loads the manifest and injects it into prompts.',
        'The coordinator writes its full conversation transcript to disk each turn and replays it byte-for-byte after a crash.',
        'Each agent prints progress to a log so a human can reconstruct and reassign the remaining work.',
        'Each agent’s session is resumed by id after the crash, relying on session persistence to restore context.'],
      why:'Structured state persistence via manifests: agents export curated state, and recovery injects it into fresh prompts. Replaying transcripts is bulky and stale-prone; sessions persist conversation, not designed world-state.' },
    { scen:1, d:1, ts:'1.3', ans:[0],
      stem:'When passing web-search output to the synthesis subagent, why use structured data (claim, excerpt, source URL, date) rather than raw prose?',
      opts:[
        'It separates content from metadata so attribution survives the handoff between agents.',
        'It is required by the Task tool, which rejects prose payloads in subagent prompts.',
        'It compresses better, letting more findings fit in the synthesis agent’s context window.',
        'It lets the synthesis agent skip reading the excerpts and rely on the claims alone.'],
      why:'Structured formats keep content separate from metadata (URLs, dates, document names), preserving attribution when context passes between agents — the foundation for cited reports. The Task tool accepts prose; the point is provenance, not a format requirement.' },
    { scen:1, d:1, ts:'1.7', ans:[0],
      stem:'From a shared codebase-analysis baseline, you want to explore two refactoring strategies independently without losing the ability to return to either. What do you use?',
      opts:[
        'Fork the analysed session twice, giving two independent branches from the same baseline.',
        'Resume the analysed session twice in sequence, asking for one strategy then the other.',
        'Start two fresh sessions and paste a written summary of the analysis into each.',
        'Run both strategies in the original session so the model can compare them directly.'],
      why:'Forking seeds a new session with a copy of the baseline history, leaving the original intact; each fork has its own id. Sequential resumes contaminate the second exploration with the first; one session mixes the two.' },

    /* ============ SCENARIO 3 — Code Generation with Claude Code (D3/D4/D1) ============ */
    { scen:2, d:3, ts:'3.2', ans:[0],
      stem:'You want a /deploy-check command available to every developer on clone. Where does it belong?',
      opts:[
        'In .claude/commands/ in the project repository, shared through version control.',
        'In ~/.claude/commands/ in each developer’s home directory.',
        'In the project root CLAUDE.md under a "Commands" heading.',
        'In a .claude/config.json file listing commands in an array.'],
      why:'Project-scoped commands live in .claude/commands/ in the repo — versioned, so every developer gets them on clone. User scope is personal; CLAUDE.md holds instructions not command definitions; the config.json array does not exist.' },
    { scen:2, d:3, ts:'3.1', ans:[1],
      stem:'A new teammate does not get the team’s coding conventions that work on the lead’s machine. Most likely cause?',
      opts:[
        'The teammate has not run /init to generate the project conventions on their clone.',
        'The conventions live in the lead’s user-level ~/.claude/CLAUDE.md, which is not shared via version control.',
        'Project CLAUDE.md files require per-machine approval before they load for a new user.',
        'The conventions are in a subdirectory CLAUDE.md that loads only for its original author.'],
      why:'User-level configuration is personal and never travels through version control. Team conventions belong at project level. The fix is moving the content, not regenerating it; CLAUDE.md files have no approval gate.' },
    { scen:2, d:3, ts:'3.3', ans:[0],
      stem:'Test files (*.test.tsx) are spread across dozens of directories and must all follow the same conventions. Most maintainable configuration?',
      opts:[
        'A .claude/rules/ file with paths frontmatter matching **/*.test.tsx, loading when a test file is read.',
        'A CLAUDE.md in each directory containing tests, each holding a copy of the conventions.',
        'A section in root CLAUDE.md, relying on Claude to notice which files are tests.',
        'A skill developers invoke manually whenever they intend to write tests.'],
      why:'A convention following a file type spread across the tree is the glob-rule case: one paths pattern covers every test file wherever it lives, loading only when relevant. Per-directory CLAUDE.md means dozens of duplicates; a skill needs manual invocation.' },
    { scen:2, d:3, ts:'3.1', ans:[0],
      stem:'What is true about the @import syntax in CLAUDE.md?',
      opts:[
        'Imported files expand at launch, so imports organise content but do not reduce context usage.',
        'Imported files load lazily on first reference, reducing tokens versus inline content.',
        'Import paths resolve relative to the working directory regardless of the importing file’s location.',
        'Imports may nest to unlimited depth since each file expands before its own imports run.'],
      why:'@import expands at launch alongside the referencing file — it is an organisation tool, not a context saver (that is path-scoped rules). Paths resolve relative to the containing file, and nesting caps at four hops.' },
    { scen:2, d:3, ts:'3.4', ans:[0],
      stem:'You must restructure a monolith into microservices — dozens of files, decisions about service boundaries. Which approach?',
      opts:[
        'Enter plan mode to explore the codebase and design the approach before making changes.',
        'Start direct execution and switch to plan mode only if unexpected complexity emerges.',
        'Use direct execution with detailed upfront instructions specifying each service’s structure.',
        'Begin with the smallest service in direct execution to learn the pattern, then plan the rest.'],
      why:'Architectural decisions plus multi-file scale are plan mode’s purpose: explore and design before committing, since wrong boundaries found late mean costly rework. The complexity is stated up front, so there is nothing to wait to discover.' },
    { scen:2, d:3, ts:'3.4', ans:[0],
      stem:'A single-file bug with a clear stack trace needs a one-line null check added. Which approach?',
      opts:[
        'Direct execution — the scope is known and the blast radius is small.',
        'Plan mode, to be safe, since any change carries some risk of unintended effects.',
        'The Explore subagent first, to map the file before touching it.',
        'Plan mode to design the fix, then direct execution to apply it.'],
      why:'Direct execution fits well-scoped, well-understood changes. Plan mode is overhead here; the scope is already clear from the stack trace. Matching mode to complexity is the skill.' },
    { scen:2, d:3, ts:'3.2', ans:[0],
      stem:'A codebase-analysis skill floods the main conversation with exploration output. Which frontmatter fixes it?',
      opts:[
        'context: fork — the skill runs in an isolated subagent context, returning only its result.',
        'allowed-tools: Read — limiting tools reduces how much the skill outputs.',
        'disable-model-invocation: true — so the skill runs only when explicitly invoked.',
        'argument-hint: "[path]" — scoping the analysis bounds the output size.'],
      why:'context: fork is packaged context isolation — the verbose exploration stays in the fork and only the summary returns. Tool restriction and invocation controls change what the skill can do or when it runs, not where its output lands.' },
    { scen:2, d:4, ts:'4.1', ans:[1],
      stem:'A CI review bot floods PRs with style nitpicks. Adding "be conservative, only report high-confidence issues" did not help. Why, and what works?',
      opts:[
        'The instruction was placed too late; move it to the top of the system prompt for more weight.',
        'Confidence adjectives adjust tone, not the boundary; define categories to report (bugs, security) versus skip (style).',
        'The model cannot vary precision at fixed temperature; lower the temperature to suppress weak findings.',
        'General instructions need repetition; restate the conservatism rule before each file is analysed.'],
      why:'"Be conservative" and "high-confidence" are thresholds without a scale — they change tone, not the boundary. Categorical report/skip criteria turn self-grading into classification, which models do reliably.' },
    { scen:2, d:4, ts:'4.6', ans:[0],
      stem:'A 14-file PR reviewed in one pass gives inconsistent depth, misses bugs, and flags a pattern in one file while approving identical code in another. Best restructuring?',
      opts:[
        'Per-file passes for local issues, then a separate integration pass for cross-file data flow.',
        'A larger-context model so all 14 files get adequate attention in one pass.',
        'Require developers to split the PR into batches of 3–4 files before review.',
        'Three independent full-PR passes, flagging only issues appearing in at least two runs.'],
      why:'The cause is attention dilution across many files in one pass. Per-file passes give consistent depth; an integration pass catches cross-file issues. A bigger window addresses capacity, not attention quality; consensus voting suppresses real single-run findings.' },
    { scen:2, d:4, ts:'4.6', ans:[0],
      stem:'Why is an independent Claude instance better than the generating session at reviewing generated code?',
      opts:[
        'The generating session retains its own reasoning and is unlikely to question its own decisions.',
        'The generating session has too little context left after generation to review thoroughly.',
        'Review and generation instructions conflict within one session, blending the two roles.',
        'An independent instance can run a stronger model, and review benefits more from capability.'],
      why:'The contamination is the retained reasoning: the assumptions that produced a bug are still in context, defending it. A fresh instance sees only the code. Self-review instructions and extended thinking do not remove context — separation does.' },
    { scen:2, d:3, ts:'3.6', ans:[0],
      stem:'Your CI job runs `claude "review this PR"` and hangs waiting for input. Correct fix?',
      opts:[
        'Add the -p flag: claude -p "review this PR".',
        'Set CLAUDE_HEADLESS=true before running the command.',
        'Redirect stdin from /dev/null so no prompt can block.',
        'Add a --batch flag to queue the work for offline processing.'],
      why:'-p (--print) runs non-interactively: process the prompt, print to stdout, exit. CLAUDE_HEADLESS and --batch do not exist; the stdin redirect is a workaround that does not address Claude Code’s interactive mode.' },
    { scen:2, d:3, ts:'3.6', ans:[0,1],
      stem:'Your CI review bot re-posts the same comments on every push, and generates low-value tests. Which practices help? (Select 2.)',
      opts:[
        'Include prior review findings in context and instruct Claude to report only new or unaddressed issues.',
        'Document testing standards, valuable-test criteria, and available fixtures in CLAUDE.md.',
        'Delete all previous bot comments before each run so only the latest findings remain.',
        'Post review output only on the first run of each PR and suppress every later pass.'],
      why:'Feeding prior findings back lets the model report only new/unaddressed issues; CLAUDE.md as CI context improves test quality and cuts low-value output. Deleting comments loses history; suppressing later passes goes blind to newly introduced issues.' },
    { scen:2, d:4, ts:'4.2', ans:[0],
      stem:'CI review keeps flagging an acceptable local pattern as a bug. Which technique reduces the false positives while preserving genuine bug detection?',
      opts:[
        'Few-shot examples distinguishing the acceptable pattern from genuine issues, with reasoning.',
        'A blanket instruction to never flag anything resembling that pattern under any circumstances.',
        'Lowering the review temperature so borderline findings are suppressed by sampling.',
        'Disabling the review category permanently so the pattern can never be flagged again.'],
      why:'Few-shot examples that contrast acceptable patterns with real issues (and show the reasoning) reduce false positives while letting the model generalise. A blanket ban also suppresses genuine bugs in that shape; permanent disabling loses the category entirely.' },
    { scen:2, d:3, ts:'3.5', ans:[0],
      stem:'You keep re-describing a data transformation in prose and Claude interprets it differently each time. Which refinement technique does the blueprint prescribe?',
      opts:[
        'Provide two or three concrete input/output example pairs demonstrating the transformation.',
        'Rewrite the prose with more precise terminology and numbered sub-steps.',
        'Ask Claude to restate the requirement in its own words before the next attempt.',
        'Split the transformation into smaller functions requested one at a time.'],
      why:'When natural language is interpreted inconsistently, concrete input/output examples are the named fix — they carry the specification unambiguously. A fourth redescription fails the way the first three did.' },
    { scen:2, d:1, ts:'1.6', ans:[0],
      stem:'"Add comprehensive tests to this legacy module" — nobody knows its structure or dependencies. Which decomposition?',
      opts:[
        'Adaptive: map structure first, identify high-impact areas, then a prioritised plan that adapts as dependencies surface.',
        'A fixed chain: list files, then write tests for each file in path order until done.',
        'Parallel fan-out: one subagent per file, each writing that file’s tests independently.',
        'Direct execution starting at the entry point, letting failures reveal what to test next.'],
      why:'The right next step depends on what mapping discovers, so subtasks cannot be enumerated up front — the adaptive case. Fixed and parallel-by-file decompositions organise around known structure while the risk lives in the unknown dependency graph.' },

    /* ============ SCENARIO 4 — Structured Data Extraction (D4/D2/D5) ============ */
    { scen:3, d:4, ts:'4.3', ans:[0],
      stem:'Why define an extraction tool with a JSON input_schema instead of prompting "respond only in valid JSON"?',
      opts:[
        'Tool arguments are generated under the schema, eliminating syntax errors, fences and preamble structurally.',
        'Tool definitions outrank user instructions, so the format requirement cannot be ignored.',
        'Anthropic validates the tool result server-side and regenerates it automatically on failure.',
        'Instructed JSON counts against output tokens while tool arguments do not.'],
      why:'The mechanism is structural: the tool call is the output, generated under the schema; with strict:true conformance is exact. A prompt instruction is a request usually honoured; a schema constrains generation itself.' },
    { scen:3, d:4, ts:'4.3', ans:[0],
      stem:'Extractions validate against a strict schema, yet some invoices have line items that do not sum to the stated total. What does this show?',
      opts:[
        'Schemas eliminate syntax errors but not semantic ones — values can fit the shape and be wrong.',
        'The schema is under-constrained; add a computed-sum field with a numeric range restriction.',
        'strict mode is off; strict schemas verify arithmetic relationships between numeric fields.',
        'The model is truncating long invoices; raising max_tokens will restore the missing items.'],
      why:'Shape versus truth: items-don’t-sum is a semantic error that validates perfectly. The fix is post-extraction validation with retry feedback, not more schema — JSON Schema cannot express cross-field arithmetic at any strictness.' },
    { scen:3, d:4, ts:'4.3', ans:[0],
      stem:'A required, non-nullable due_date field produces plausible dates for invoices that state none. Mechanism and fix?',
      opts:[
        'A required field orders a value, so the model fabricates one; make the field nullable so absence is representable.',
        'The model confuses issue and due dates; add a disambiguating description to each date field.',
        'Temperature is too high for extraction; lower it so the model stays closer to the source.',
        'The document exceeds context; chunk it before extracting so the date is not guessed.'],
      why:'A required field is an instruction to produce a value; when the document has none, compliance means fabrication. Nullable fields give absence a legal representation, and the model prefers the honest null. The schema itself manufactured the hallucination.' },
    { scen:3, d:4, ts:'4.3', ans:[0],
      stem:'Documents of unknown type arrive, each with its own extraction tool. The pipeline parses tool_use blocks and must never receive prose. Which tool_choice?',
      opts:[
        '"any" — a tool call is guaranteed and the model selects the schema that fits the document.',
        'Forced selection of the most common type, with the others as fallbacks.',
        '"auto" with a firm instruction that the model must always call an extraction tool.',
        '"any" with parallel tool use enabled so multiple schemas can be tried at once.'],
      why:'tool_choice "any" guarantees structured output when multiple schemas exist and the document type is unknown — guarantee the call, delegate the choice. "auto" can still return prose; forcing one schema mishandles the other document types.' },
    { scen:3, d:4, ts:'4.4', ans:[0],
      stem:'An extraction fails Pydantic validation. What should the retry request contain?',
      opts:[
        'The original document, the failed extraction, and the specific validation errors.',
        'The original document and fresh instructions, omitting the failed attempt to avoid anchoring.',
        'Only the specific validation errors, to keep the retry request small.',
        'The failed extraction and a general instruction to re-examine every field more carefully.'],
      why:'All three: ground truth to re-read, the model’s own output to compare, and the specific errors pointing at the discrepancy. This makes the retry a diagnosis. Omitting the failed attempt removes the evidence self-correction needs.' },
    { scen:3, d:4, ts:'4.4', ans:[0],
      stem:'Two failures: (1) dates rendered as "March 3rd" instead of ISO 8601; (2) a PO number that exists only in a separate document you never provided. What is true about retrying?',
      opts:[
        'The date failure is retryable with error feedback; the PO number is not, since it is absent from the source.',
        'Both are retryable; the PO number just needs several more attempts to be located.',
        'Neither is retryable; both require regenerating the extraction schema first.',
        'The PO failure is retryable since retries broaden the search; the date needs a schema change.'],
      why:'Retries fix format/structural errors — information present but rendered wrong. They cannot fix absence: each retry returns an honest null or fabricates. First question before retrying: is the information actually in what was sent?' },
    { scen:3, d:4, ts:'4.4', ans:[0],
      stem:'You want mismatches between line items and the stated total to be machine-detectable without human review. Which design?',
      opts:[
        'Extract calculated_total alongside stated_total so a discrepancy between them flags itself.',
        'Add a regex constraint to the total field ensuring it is a well-formed currency amount.',
        'Run each invoice through two extraction passes and diff the two records.',
        'Require the model to show its arithmetic in a scratchpad field for later spot-checks.'],
      why:'Self-correction by construction: the model computes the sum as its own field and validation compares two numbers. A regex validates format not arithmetic; two passes can share the same error; a scratchpad still needs a human to check it.' },
    { scen:3, d:4, ts:'4.3', ans:[0],
      stem:'Unusual expenses keep landing in the nearest wrong category of a closed enum. Blueprint’s schema pattern?',
      opts:[
        'Add an "other" enum value plus a detail string, and an "unclear" value for ambiguous cases.',
        'Remove the enum and use free text, normalising the values in post-processing.',
        'Expand the enum until every observed expense type has its own named category.',
        'Return the top two candidate categories with confidence scores and pick the higher.'],
      why:'Enum + "other" + detail keeps the taxonomy stable while giving oddballs an honest home; "unclear" handles ambiguity. Free text fragments into many spellings; expanding forever chases a moving target.' },
    { scen:3, d:4, ts:'4.5', ans:[0],
      stem:'Two workflows: a blocking pre-merge check developers wait on, and an overnight technical-debt report. Your manager wants both on the Message Batches API for the 50% saving. Evaluate.',
      opts:[
        'Batch the overnight report only; keep real-time calls for the blocking pre-merge check.',
        'Batch both, adding status polling so completion is detected as soon as possible.',
        'Keep both real-time, since batch results can arrive out of order across submissions.',
        'Batch both, with a timeout fallback to real-time if a batch runs too long.'],
      why:'The Batch API is 50% cheaper but has up to 24h processing and no latency SLA — unusable for a blocking check, ideal for an overnight job. Ordering is a non-issue (custom_id correlates); a fallback adds complexity where matching each workflow to its API suffices.' },
    { scen:3, d:4, ts:'4.5', ans:[0],
      stem:'A 100-document batch returns 6 failures: 4 exceeded context, 2 were transient. Correct follow-up?',
      opts:[
        'Resubmit only the 6 failures by custom_id, chunking the 4 oversized ones first.',
        'Resubmit all 100 pre-chunked so every request fits comfortably.',
        'Resubmit the 6 through the synchronous API, since batch retries repeat failures.',
        'Resubmit the 4 oversized documents chunked and accept the 2 transient losses.'],
      why:'custom_id identifies the failed pairs; resubmit only those with cause-addressing changes (chunk the oversized, plain-retry the transient). Resubmitting all 100 doubles cost; abandoning the 2 transient loses recoverable work.' },
    { scen:3, d:2, ts:'2.5', ans:[0],
      stem:'You must rename a constant that appears 11 times in one file. Most efficient built-in approach?',
      opts:[
        'Edit with replace_all set to true, old string to new string.',
        'Read the whole file, then Write it back with all 11 occurrences changed.',
        'Bash with sed to perform a global substitution across the file.',
        'Eleven Edit calls, each anchored with unique surrounding context.'],
      why:'Same change at every occurrence in one file is what replace_all exists for — one call, diff only. Read+Write is the recovery path for a different case (non-unique anchor on a single change); sed is the standing distractor.' },
    { scen:3, d:2, ts:'2.5', ans:[0],
      stem:'Edit fails on a config file because the anchor text appears three times, and you want to change only one occurrence. Correct next step?',
      opts:[
        'Read the full file, then Write it back with only that one line modified.',
        'Retry Edit with replace_all so the non-unique match no longer fails.',
        'Use Bash with sed to target the specific occurrence by line number.',
        'Use Grep to find the occurrence, then re-run the same Edit unchanged.'],
      why:'Non-unique anchor with a single intended change → Read + Write, the documented fallback. replace_all would change all three; sed is the distractor; Grep locates but does not make the anchor unique.' },
    { scen:3, d:2, ts:'2.5', ans:[1],
      stem:'You need every file whose contents call parseInvoice, and separately every file named like *.schema.json. Which tools?',
      opts:[
        'Glob for the callers (call sites follow patterns); Grep for the schema files (they mention "schema").',
        'Grep for the callers (a content search); Glob for the schema files (a path pattern).',
        'Grep for both — one search for the function, one for files mentioning schema.',
        'Read candidate files for the callers; Glob for the schema files by name.'],
      why:'Grep searches file contents — a call site is text inside files. Glob matches paths — *.schema.json finds files by name. Content question → Grep; naming question → Glob.' },
    { scen:3, d:5, ts:'5.5', ans:[0],
      stem:'The pipeline reports 97% aggregate accuracy and the team proposes auto-approving all extractions above 95% confidence. Critical risk?',
      opts:[
        'Aggregate accuracy can mask poor per-type performance, and the confidence scores are uncalibrated.',
        'The model will grow overconfident over time as it processes more documents, needing retraining.',
        'The 95% threshold is too low and should be raised to 99% before any automation.',
        'Automation always needs human review regardless of confidence, so the proposal is fundamentally flawed.'],
      why:'Two measurement gaps: 97% overall can hide a 40% error rate on one document type, and uncalibrated confidence routes nothing. Fix the measurement (segment validation + calibration), then automate. Blanket rejection abandons a legitimate goal; a higher uncalibrated threshold means nothing.' },
    { scen:3, d:5, ts:'5.5', ans:[0,1,4],
      stem:'Before reducing human review on high-confidence extractions, which safeguards are required? (Select 3.)',
      opts:[
        'Calibrate field-level confidence thresholds against a labelled validation set.',
        'Keep stratified random samples of high-confidence output under review to measure error and catch novel patterns.',
        'Grow the review team until every extraction can be checked by hand.',
        'Rely on the aggregate accuracy once it has held steady for a full quarter.',
        'Verify accuracy separately by document type and field segment, not just in aggregate.'],
      why:'The TS 5.5 triad: calibrate per field on labelled data, validate by segment, and keep a stratified audit sample after automating. Reviewing everything abandons the routing goal; a stable aggregate still hides per-segment weakness.' },
    { scen:3, d:5, ts:'5.5', ans:[0],
      stem:'A calibrated pipeline (90% auto-approve, validated three months) receives contracts in an unfamiliar two-column layout and reports 92% confidence. Appropriate safeguard?',
      opts:[
        'Route the novel-layout documents to human review until enough labelled examples extend the calibration.',
        'Auto-approve, since 92% exceeds the validated threshold and calibration held for a quarter.',
        'Raise the global threshold to 98% for all document types until the new layout matures.',
        'Reject the documents and ask the client to resubmit in a supported format.'],
      why:'Calibration is valid only for distributions in the labelled set. On an unseen layout the reported 92% is uncalibrated — possibly systematically overconfident. Novel inputs route to review, which also builds the labelled sample. Raising the global threshold punishes well-calibrated formats.' },
    { scen:3, d:4, ts:'4.2', ans:[0],
      stem:'Extraction returns null for a "methodology" field whenever a paper describes methods in prose rather than a labelled section, though the content is present. Named fix?',
      opts:[
        'Few-shot examples showing correct extraction from varied structures, including embedded prose.',
        'Make the methodology field required and non-nullable so the model must find something.',
        'A validation-retry loop that re-sends the document whenever methodology comes back null.',
        'Chunk each paper by section and run extraction on every chunk in parallel.'],
      why:'Few-shot examples for varied document structures are the named fix for empty/null extraction of present fields — the model learns the content can live in unfamiliar shapes. A required field manufactures hallucinations; retrying the same failure adds nothing new.' },
    { scen:3, d:2, ts:'2.2', ans:[0],
      stem:'An extraction MCP tool hits a rate limit on the source API. What error metadata best enables the agent to recover?',
      opts:[
        'errorCategory "transient", isRetryable true, and a human-readable note that a retry after backoff should succeed.',
        'errorCategory "validation", isRetryable false, since the request itself must have been malformed.',
        'A generic "extraction failed" string so the agent falls back to a simpler approach.',
        'errorCategory "permission", isRetryable false, so the agent escalates to a human immediately.'],
      why:'A rate limit is a transient error: retryable after backoff. Structured metadata (category, isRetryable, description) lets the agent retry intelligently rather than misclassifying it as a validation or permission failure and giving up.' },
    { scen:3, d:5, ts:'5.1', ans:[0],
      stem:'A downstream aggregation agent runs out of context receiving upstream extractors’ full prose plus reasoning chains. Blueprint’s change?',
      opts:[
        'Modify the upstream extractors to return structured data — key facts, scores — instead of verbose narrative.',
        'Give the aggregation agent a larger budget by trimming its own system prompt to a minimum.',
        'Split aggregation into sequential passes, each consuming a fraction of the upstream output.',
        'Insert a summarisation agent to compress the upstream output before aggregation.'],
      why:'Fix the producer: upstream agents return structured findings, which cost a fraction of prose-plus-reasoning and preserve the needed fields. A summarisation hop is lossy — flattening specifics and stripping attribution, the exact TS 5.1/5.6 failures.' },
    { scen:3, d:2, ts:'2.4', ans:[0],
      stem:'A shared extraction MCP server must reach every developer on clone but needs each one’s own API key. Configuration?',
      opts:[
        'Define it in project .mcp.json with ${API_KEY} expansion, resolved from each developer’s environment.',
        'Define it in each developer’s ~/.claude.json and document the setup in the README.',
        'Define it in project .mcp.json with the key inline, restricting repo access to the team.',
        'Define it in ~/.claude.json with ${API_KEY} expansion and commit that file.'],
      why:'Shared tooling goes in project .mcp.json (versioned, arrives on clone); env-var expansion keeps the credential out of the commit. ~/.claude.json is never committed; an inline key in a repo is a leaked secret.' },
    { scen:3, d:1, ts:'1.6', ans:[0],
      stem:'A multi-aspect document review should check formatting, citations, and factual consistency. These aspects are known in advance and independent. Which decomposition?',
      opts:[
        'Prompt chaining: a fixed sequence of focused passes, one per aspect.',
        'Adaptive decomposition, generating aspects to check based on each document’s content.',
        'A single pass instructing the model to check all three aspects together thoroughly.',
        'One subagent that decides at runtime which aspects are worth checking per document.'],
      why:'When the aspects are known up front, a fixed prompt chain is the right shape — predictable, testable, and it avoids the attention dilution of one combined pass. Adaptive decomposition is for open-ended work where the next step depends on findings.' },

    /* ============ extra D1/D2/D3 to reach official weighting ============ */
    { scen:0, d:1, ts:'1.5', ans:[0],
      stem:'A hook is needed that blocks a policy-violating tool call before it executes and redirects to an alternative workflow. Which hook type?',
      opts:[
        'PreToolUse — it intercepts the outgoing call and can deny or rewrite it before execution.',
        'PostToolUse — it inspects the result and can substitute a compliant one after the fact.',
        'A Stop hook — it runs when the agent finishes and can veto non-compliant outcomes.',
        'A SessionStart hook — it configures policy once at the beginning of each session.'],
      why:'PreToolUse fires before a tool runs and can block (permissionDecision: deny) or rewrite the call. PostToolUse is too late — the action already happened. Stop and SessionStart fire at the wrong lifecycle points.' },
    { scen:1, d:2, ts:'2.1', ans:[0],
      stem:'Two subagent tools, analyze_content and analyze_document, have near-identical descriptions and the agent routes between them at random. Rewriting descriptions helped only partly. Next escalation?',
      opts:[
        'Rename to encode distinct purposes — e.g. analyze_content becomes extract_web_results with a web-specific description.',
        'Set tool_choice "any" so the model must commit to one of the two rather than answer in prose.',
        'Add a PreToolUse hook that swaps the call to the other tool when the input type looks wrong.',
        'Lower the request temperature so selection between the similar tools is more consistent.'],
      why:'The escalation ladder: rewrite descriptions → rename to kill conceptual overlap → split generic tools. Two tools sharing the verb "analyze" stay confusable however good the descriptions; the name itself must carry selection signal.' },
    { scen:2, d:3, ts:'3.1', ans:[0],
      stem:'Claude behaves inconsistently across sessions — sometimes following a convention, sometimes not. Which first diagnostic step?',
      opts:[
        'Run /memory to verify which memory files exist and are loaded.',
        'Run /compact to clear context and force every memory file to reload.',
        'Delete ~/.claude/CLAUDE.md to rule out the user level, then retest.',
        'Add the convention via --append-system-prompt on every launch.'],
      why:'Diagnose before surgery: /memory shows which files exist and load, where hierarchy bugs reveal themselves (wrong level, contradictions, non-matching globs). The other options change state before establishing what is actually loaded.' },
    { scen:2, d:3, ts:'3.5', ans:[0],
      stem:'Two of Claude’s output problems are a pagination bug and an off-by-one in the same loop where fixing either affects the other; a third is an unrelated CSS typo. How to sequence?',
      opts:[
        'Describe both loop problems in one message; fix the CSS typo separately.',
        'Fix all three in one message so no context is lost between iterations.',
        'Fix each of the three sequentially, verifying each before the next.',
        'Fix the CSS typo first as the quick win, then the two loop problems one at a time.'],
      why:'Interacting problems go in one message so the fix accounts for both; independent problems are handled separately. The two loop issues interact; the CSS typo is independent.' },
    { scen:3, d:4, ts:'4.1', ans:[0],
      stem:'One review category (architecture advice) has a 55% false-positive rate; the others are accurate. Developers now dismiss all findings. Blueprint’s prescription?',
      opts:[
        'Temporarily disable the architecture category to restore trust, and improve its prompt before re-enabling.',
        'Keep all categories active but rank architecture findings last so accurate ones read first.',
        'Attach a confidence score to each architecture finding so developers can weigh them.',
        'Route architecture findings to a senior channel where false positives do less harm.'],
      why:'High-FP categories undermine trust in accurate ones — the bot is judged as a whole. The named move is temporary disablement plus offline improvement: a silent category costs less than a noisy wrong one. Confidence scores reach for the unreliable proxy again.' },
    { scen:1, d:1, ts:'1.7', ans:[0],
      stem:'You resume a week-old analysis session and the agent confidently references function signatures that no longer exist. Underlying issue and most reliable fix?',
      opts:[
        'Sessions restore stale tool results; start fresh and inject a structured summary of the still-true conclusions.',
        'Sessions expired and truncated the transcript; resume again with a longer retention setting.',
        'Sessions restore the old filesystem state; discard the working-tree changes to match.',
        'Sessions cannot resume across processes; recapture the id and resume once more.'],
      why:'A session persists conversation, not the filesystem. Resuming replays stale tool results the model has no reason to doubt. When results are stale, a fresh session with a summary of the still-valid conclusions is more reliable.' },
    { scen:0, d:1, ts:'1.2', ans:[0],
      stem:'Why should all inter-subagent communication route through the coordinator rather than agents talking directly?',
      opts:[
        'For observability, consistent error handling, and controlled information flow.',
        'Because subagents share a context window that would otherwise desynchronise.',
        'Because direct messaging between subagents is not technically supported at all.',
        'To compress messages as they pass through, reducing total token cost.'],
      why:'Hub-and-spoke exists for observability, consistent error handling, and controlled information flow — the coordinator decides what each subagent sees. Subagents never share a context window, and routing through the coordinator generally costs more tokens, not fewer.' },
    { scen:3, d:4, ts:'4.3', ans:[0],
      stem:'Source documents write dates five different ways but downstream needs ISO 8601. Where does normalisation logic belong?',
      opts:[
        'In the prompt, as explicit format rules alongside the strict output schema.',
        'In the schema, as a regex on the date field so non-ISO values fail validation.',
        'In deterministic post-processing code, since date parsing should not use a model.',
        'In a PostToolUse hook that rewrites whatever date format extraction produces.'],
      why:'The schema cannot express conversions — it locks clean output while the prompt handles messy input, so format rules go in the prompt. Ambiguous inputs like "3/4/26" need the document’s context to resolve, which the model reading it supplies; a regex can reject but not convert.' }
  ]
};
